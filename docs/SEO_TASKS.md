# SEO Engineering Backlog — badifrei.ch

> Generated by PM agent + architect review · March 2026  
> Source: [SEO_REPORT.md](./SEO_REPORT.md)

---

## Status Legend
| Symbol | Meaning |
|--------|---------|
| ✅ | Done |
| 🔄 | In progress |
| ⬜ | To do |

---

## 🔴 P0 — Critical / Do Now

### ✅ TASK-SEO-007 — Migrate URL structure to /bad/{uid}
**Effort:** M · **Done:** 2026-03-03

Renamed FastAPI route from `/dashboard/pools/{uid}` → `/bad/{uid}`. Updated sitemap, canonical tags, JSON-LD schema URLs, index.html links, and robots.txt (`Disallow: /dashboard/`). No 301 redirects needed (site < 24h old).

---

### ⬜ TASK-SEO-001 — SSR occupancy data into pool page templates
**Effort:** L · **Dependencies:** none

Pass `current_occupancy`, `today_predictions_json`, `typical_quiet_time`, `typical_busy_time`, and `typical_quiet_day` from the FastAPI pool route into the Jinja2 template. Render a visible text summary (e.g. "Aktuelle Auslastung: 42% — 🟢 Wenig los") and a "Beste Besuchszeiten" section in server-rendered HTML. Chart.js initialises from embedded JSON instead of making an extra API call.

**Why it matters:** Google currently sees only nav + footer on pool pages — no SSR content means de-indexing risk.

**Acceptance criteria:**
- `curl -s https://badifrei.ch/bad/<uid>` returns occupancy % and busy-time text in raw HTML
- Lighthouse "page has crawlable content" passes on all 20 pool pages
- Chart.js initialises from embedded JSON (no extra `/api/history` call on load)
- Google Search Console "URL Inspection → Rendered HTML" shows occupancy text

---

### 🔄 TASK-SEO-002 — Add unique meta description to all 20 pool pages
**Effort:** S · **Dependencies:** none

`pool.html` does not override `{% block description %}` — all 20 pool pages share the same generic description. Override it with: `{pool.name} {city} – Auslastung live. Wann ist es am wenigsten voll? KI-Prognose, Öffnungszeiten & Kapazität.`

**Why it matters:** Identical descriptions suppress CTR and signal duplicate content to Google.

**Acceptance criteria:**
- Each of the 20 pool pages has a unique `<meta name="description">`
- No two pool pages share an identical description string
- Description length is 140–160 characters
- Homepage description unchanged

---

### ⬜ TASK-SEO-003 — Fix og:url and og:description per pool page
**Effort:** S · **Dependencies:** TASK-SEO-007 ✅

In `base.html`, wrap `og:url` and `og:description` in Jinja2 template blocks. In `pool.html`, override with pool-specific values using the `/bad/{uid}` path.

**Why it matters:** Social crawlers currently report all pool pages as the homepage — destroying pool-level social traffic.

**Acceptance criteria:**
- `og:url` on any pool page matches that pool's canonical URL (not homepage)
- `og:description` differs across sampled pool pages
- `og:url` uses `/bad/{uid}` path
- No Jinja2 template errors

---

### ⬜ TASK-SEO-004 — Fix SportsActivityLocation schema: dynamic fields + missing data
**Effort:** M · **Dependencies:** TASK-SEO-007 ✅

Replace hardcoded `"description": "Schwimmbad in Zürich"` with `{pool.type} in {pool.city}`. Add missing schema fields: `openingHoursSpecification`, `maximumAttendeeCapacity`, `geo` (lat/lon), `image`.

**Why it matters:** Wrong city in schema hurts Luzern/Zug pools; missing fields reduce rich result eligibility.

**Acceptance criteria:**
- Google Rich Results Test passes for all 20 pool pages (no errors)
- Schema description for a Luzern pool does not contain "Zürich"
- `openingHoursSpecification`, `maximumAttendeeCapacity`, `geo`, `image` present and valid
- Schema `url` field matches canonical URL

---

### ⬜ TASK-SEO-005 — Add security response headers
**Effort:** S · **Dependencies:** none

> **Note:** `SecurityHeadersMiddleware` already exists in `main.py` but uses `X-Frame-Options: DENY` (should be `SAMEORIGIN`) and enforces CSP (should start as `Report-Only`). Needs tuning.

Ensure these headers are present and correct:
- `X-Frame-Options: SAMEORIGIN`
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy: geolocation=(), microphone=()`
- `Content-Security-Policy-Report-Only` (permissive, report-only mode)

**Why it matters:** Incorrect security headers expose users and harm E-E-A-T trust signals.

**Acceptance criteria:**
- `curl -I https://badifrei.ch/` returns all five headers
- CSP is `Report-Only`, not enforced
- Chart.js loads without CSP violations
- SecurityHeaders.com scores B or above

---

## 🟠 P1 — High / Do Soon

### ⬜ TASK-SEO-006 — Update title tags on homepage and pool pages
**Effort:** S · **Dependencies:** none

- Homepage: `"badifrei.ch – Zürcher Bäder live"` → `"Zürcher Schwimmbäder – Auslastung live & Prognose | badifrei.ch"`
- Pool pages: `"{name} – badifrei.ch"` → `"{pool.name} {city} – Auslastung & Beste Besuchszeit | badifrei.ch"`

**Why it matters:** Title tags are the highest-weight on-page ranking signal; current titles omit primary intent keywords.

**Acceptance criteria:**
- Homepage title contains "Schwimmbäder" and "Prognose"
- All 20 pool titles follow the formula and include the city
- No title exceeds 60 characters

---

### ⬜ TASK-SEO-008 — Add FAQPage schema to all pool pages
**Effort:** M · **Dependencies:** TASK-SEO-001

Generate 3–4 FAQ entries per pool from server-side data (ML typical quiet/busy times, capacity, opening hours). Render as JSON-LD `FAQPage` schema alongside `SportsActivityLocation`.

**Why it matters:** FAQPage enables rich result accordions in SERPs — direct CTR boost.

**Acceptance criteria:**
- Google Rich Results Test validates FAQPage on all pool pages
- At least 3 FAQ entries per pool, data-driven (not placeholder text)
- No JSON-LD validation errors

---

### ⬜ TASK-SEO-009 — Update sitemap.xml with lastmod, changefreq, priority
**Effort:** XS · **Dependencies:** TASK-SEO-007 ✅

Add to every sitemap entry:
- `<lastmod>` — today's date (ISO 8601)
- `<changefreq>always</changefreq>` for pool pages, `daily` for homepage
- `<priority>1.0</priority>` for homepage, `0.8` for pools

**Why it matters:** Without crawl hints Googlebot deprioritises real-time pages, causing stale/missing index entries.

**Acceptance criteria:**
- All 21 sitemap entries have `lastmod`, `changefreq`, `priority`
- Homepage is priority 1.0, pools are 0.8
- Sitemap validates at xml-sitemaps.com

---

### ⬜ TASK-SEO-010 — Update robots.txt with full Disallow list
**Effort:** XS · **Dependencies:** TASK-SEO-007 ✅

> **Note:** `Disallow: /dashboard/` already added by SEO-007. Still missing: `/api/`, `/predict/`, `/health`.

Add `Disallow` rules for `/api/`, `/predict/`, `/health` to prevent crawl budget waste.

**Acceptance criteria:**
- robots.txt contains all four Disallow rules
- `/bad/` pool pages are NOT disallowed
- Google Search Console robots.txt Tester confirms blocked paths

---

### ⬜ TASK-SEO-011 — Fix homepage H1 and add H2 section subheadings
**Effort:** S · **Dependencies:** none

- H1: `"Zürcher Bäder 🏊"` → `"Zürcher Schwimmbäder – Auslastung live & Prognose"`
- Add H2 subheadings for pool type sections: "Freibäder", "Hallenbäder", "Weitere Städte"

**Why it matters:** Keyword-rich H1 + logical H2 hierarchy improves topical relevance signals.

**Acceptance criteria:**
- Homepage has exactly one `<h1>` containing "Schwimmbäder" and "Prognose"
- At least 2 `<h2>` elements grouping pool sections
- No other page uses the same H1 text

---

### ⬜ TASK-SEO-012 — SSR opening hours table and capacity on pool pages
**Effort:** M · **Dependencies:** TASK-SEO-001

> **Note:** Opening hours table already exists in `pool.html` as SSR! Verify it's crawlable. Capacity (`pool.max_capacity`) is also already rendered. This task may be partially done — verify before starting.

Ensure opening hours `<table>` and max capacity text are in initial server-rendered HTML (not JS-injected).

**Acceptance criteria:**
- `curl` of a pool page returns `<table>` with opening hours
- Capacity text (e.g. "Max. Kapazität: 500 Personen") in plain HTML
- Data matches backend source of truth (spot-check 3 pools)

---

### ⬜ TASK-SEO-013 — Fix Chart.js CLS and performance issues
**Effort:** S · **Dependencies:** none

- Add explicit `height: 400px` to `.chart-wrap` container to prevent CLS
- Add `defer` attribute to Chart.js `<script>` tag (CDN load)
- Add `<link rel="preload">` for the primary API call
- Consider SSR of initial chart data to eliminate first API round-trip

**Why it matters:** LCP likely 3-5s (threshold <2.5s); CLS from unsized chart hurts Core Web Vitals score.

**Acceptance criteria:**
- `.chart-wrap` has explicit height in CSS
- Chart.js script tag has `defer`
- Lighthouse CLS score < 0.1
- Lighthouse LCP < 2.5s (or measurably improved)

---

## 🟡 P2 — Medium / Plan It

### ⬜ TASK-SEO-014 — Add pool description text (100–200 words per pool)
**Effort:** L · **Dependencies:** none

Add static, unique, factual description text per pool (type, facilities, location, public transport). Can be stored in `pool_metadata.json` and rendered SSR.

**Why it matters:** Zero long-form crawlable text = no topical authority signal for Google.

---

### ⬜ TASK-SEO-015 — Add "Beste Besuchszeiten" section with ML-derived insights
**Effort:** M · **Dependencies:** TASK-SEO-001

Pre-compute typical busy/quiet day+time patterns from historical ML data. Render as a human-readable SSR text block per pool page.

**Why it matters:** Unique, data-driven content — the core pSEO value proposition of the site.

---

### ⬜ TASK-SEO-016 — Add internal linking between pool pages
**Effort:** S · **Dependencies:** none

Add "Ähnliche Bäder" or "Weitere Zürich Bäder" section on each pool page with 2–3 links to related pools (same city, same type).

**Why it matters:** No internal links between pool pages = no PageRank distribution; Google crawls pool pages in isolation.

---

### ⬜ TASK-SEO-017 — Verify static assets (og:image, apple-touch-icon)
**Effort:** XS · **Dependencies:** none

Verify `/static/og-preview.jpg` and `/static/apple-touch-icon.png` both return HTTP 200.

---

## 🔵 P3 — Low / Nice to Have

### ⬜ TASK-SEO-018 — Create seasonal / blog content
**Effort:** XL · **Dependencies:** TASK-SEO-014, TASK-SEO-015

Create 1–2 seasonal content articles per year: "Freibad Saison 2026 — Eröffnungsdaten", "Hitzewelle: Welche Badi ist heute am wenigsten voll?". Simple blog/news section.

---

### ⬜ TASK-SEO-019 — District/Kreis hub pages
**Effort:** L · **Dependencies:** TASK-SEO-014

Add `district`/`kreis` metadata to pool_metadata.json. Create district hub pages targeting "Schwimmbad Wiedikon", "Badi Zürich West" etc.

---

### ⬜ TASK-SEO-020 — "Jetzt geöffnet" filtered homepage view
**Effort:** M · **Dependencies:** none

Add a filtered view of the homepage showing only currently open pools. Targets "offene Bäder Zürich jetzt" search intent.

---

### ⬜ TASK-SEO-021 — Link building outreach
**Effort:** L · **Dependencies:** none

- Submit to Google Maps / Apple Maps
- Reach out to Zürich city leisure portals
- Data partnership with Züriplan, Outdooractive
- Press outreach around the ML prediction angle

---

## Summary

| Priority | Total | Done | Remaining |
|----------|-------|------|-----------|
| P0 Critical | 6 | 1 (SEO-007) | 5 |
| P1 High | 8 | 0 | 8 |
| P2 Medium | 4 | 0 | 4 |
| P3 Low | 4 | 0 | 4 |
| **Total** | **22** | **1** | **21** |

**Current SEO score estimate:** ~25/100  
**Projected after P0+P1:** ~65/100  
**Projected after all tasks:** ~85/100
